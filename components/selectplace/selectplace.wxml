<picker mode="multiSelector" bindchange="onSelectChange" bindcolumnchange="onColumnChange" value="{{placeIndex}}" range="{{placeArray}}" disabled="{{disabled}}">
  <view class="picker {{style}}">{{placeTxt}}</view>
</picker>